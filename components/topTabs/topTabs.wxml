<view>
  <view class="tab_title">
    <view class="{{item.active?'active':''}}" wx:for="{{tabs_data}}" wx:key="id" bindtap="onTab" data-index="{{index}}">
    {{item.name}}
    </view>
  </view>
  <view class="tab_content">
  <slot></slot>
  </view>
</view>
